// CHALLENGE 5: MAX CHARACTER
// Return the character that is most common in a string
// ex. maxCharacter('javascript') == 'a'
function maxCharacter(str) {
  for (let i = 0; i < str.length; i++) {
    // console.log(str.charAt(i));
    if (str.charAt([0]) === str.charAt(i)) {
      console.log(charAt(i));
    }
  }
  // loop the string does the char at i match the current char if so , make it a variable named the letter and increment it
  // continue on to the end and re start with the second index of the string ,repeat
  // when at last index of string compare the value of the letter variables and turn the name of the variable with the highest value
}

maxCharacter("javascript");
